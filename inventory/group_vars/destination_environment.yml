---
# Destination Environment Variables
# Corporate Standard - Destination API Configuration

# Destination Tenant Configuration
dst_tenant: "{{ lookup('env', 'DST_TENANT') | default('https://sivahvm.verify.ibm.com', true) }}"
dst_client_id: "{{ lookup('env', 'DST_CLIENT_ID') | default('f689a419-2d4d-4fe3-a73b-a51158d25c0b', true) }}"
dst_client_secret: "{{ lookup('env', 'DST_CLIENT_SECRET') | default('tfuEmlfMzuMVTXSK', true) }}"

#dst_tenant: "https://sivahvm.verify.ibm.com"
#dst_client_id: "aabbccddeeff00112233"
#dst_client_secret: "zzxx112233"

# Destination API Endpoints
dst_token_url: "{{ dst_tenant }}/oauth2/token"
dst_applications_url: "{{ dst_tenant }}/v1.0/applications"
dst_attributes_url: "{{ dst_tenant }}/v1.0/attributes"

# Destination Environment Specific Settings
dst_environment_name: "development"
dst_region: "US-WEST"

# OAuth Scopes for Destination
dst_oauth_scope: "openid test xyz"

# Made with Bob
